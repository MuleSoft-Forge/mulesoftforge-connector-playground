{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TaskQueryParams",
  "description": "Parameters for querying a task in A2A protocol",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier of the task to query"
    },
    "pushNotificationConfigId": {
      "type": "string",
      "description": "Server generated id of the push notification config"
    },
    "historyLength": {
      "type": "integer",
      "description": "Number of history entries to include in the response"
    },
    "metadata": {
      "type": "object",
      "description": "Additional metadata for the query",
      "additionalProperties": true
    }
  },
  "required": ["id"]
} 